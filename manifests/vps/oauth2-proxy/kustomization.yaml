---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- referencegrant-oauth2-proxy.yaml
- helmchart.yaml

generators:
- |-
  apiVersion: viaduct.ai/v1
  kind: ksops
  metadata:
    name: oauth2-proxy
    annotations:
      config.kubernetes.io/function: |
        exec:
          path: ksops
  files:
  - ./secret-oauth-proxy-dex.enc.yaml
